{
  "name": "${project.name}",
  "version": "${project.version}",
  "description": "${project.description}",
  "platform": "complete",
  "dependencies": [
    {
      "name": "app-acquisitions",
      "version": "^1.2.0-SNAPSHOT",
      "preRelease": "only"
    }
  ],
  "modules": [
    {
      "name": "mod-mosaic",
        "version": "latest",
        "preRelease": "only"
    }
  ],
  "uiModules": [
    {
      "name": "folio_mosaic-settings",
        "version": "latest",
        "preRelease": "only"
    }
  ]
}